architecture:
  d_input: 1
  d_model: 10
  n_head: 2
  n_layers: 2
  max_len: 5000
  dim_feedforward: 9
  d_output: 6  # Number of s values (set from data.yaml mgf.num_points)  dim_feedforward: 256  # Feedforward dimension (default: 4 * d_model = 256)  dim_feedforward: 4